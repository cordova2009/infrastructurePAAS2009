###查询首页招标项目概况接口

查询用户所有标的的总概况

http请求方式: post

    http://ip:port/if/tender/queryObjectIndexSurvey


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询首页招标项目概况成功",
    "objectNum":35,
    "amount":45647689
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询首页招标项目概况失败，其它错误"}


参数|是否必须|说明
----|----|-----
objectNum|是|项目数量
amount|是|交易金额


###查询首页招标项目列表接口

进入首页，查看招标列表


http请求方式: post

    http://ip:port/if/tender/queryIndexObjectList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询招标公告列表成功",
    "list":[{
	"projectCategoryId":3,
	"objectId":"3456"
	"objectName":"7665",
	"companyShortName":"456",
	"objectEvaluate":30000000,
	"objectPredictStartTime":"2015-03-24",
	"objectPredictEndTime":"2015-08-30"}]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
projectCategoryId|是|工程类别
objectId|是|标的id
objectName|是|标的名称
companyShortName|是|公司简称
objectEvaluate|是|标的估计
objectPredictStartTime|是|标的计划开始时间
objectPredictEndTime|是|标的计划完成时间



###查询首页中标结果概况接口

查询用户所有标的的总概况

http请求方式: post

    http://ip:port/if/bid/queryBidIndexSurvey


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询首页招标项目概况成功",
    "bidNum":35,
    "amount":45647689
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询首页招标项目概况失败，其它错误"}


参数|是否必须|说明
----|----|-----
bidNum|是|中标公告数量
amount|是|中标累计金额


###查询首页中标结果项目列表接口

进入首页，查看招标列表


http请求方式: post

    http://ip:port/if/bid/queryBidIndexList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询招标公告列表成功",
    "list":[{
	"projectCategoryId":3,
	"objectName":"7665",
	"objectId":"45657"
	"bideeName":"中标公司简称",
	"biderName":"招标公司简称",
	"objectOffer":30000000}]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
projectCategoryId|是|工程类别
objectName|是|标的名称
objectId|是|标的Id
companyShortName|是|公司简称
objectOffer|是|中标金额
objectPredictStartTime|是|标的计划开始时间
objectPredictEndTime|是|标的计划完成时间


###查询首页投标人概况接口

查询首页投标人概况

http请求方式: post

    http://ip:port/if/userInfo/queryBiderIndexSurvey


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询首页招标项目概况成功",
    "stairBiderNum":35,
    "secondBiderNum":45647689
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询首页招标项目概况失败，其它错误"}


参数|是否必须|说明
----|----|-----
stairBiderNum|是|一级承包商数量
secondBiderNum|是|二级承包商数量


###查询首页投标人推荐列表接口

查询首页投标人推荐列表


http请求方式: post

    http://ip:port/if/userInfo/queryIndexBidList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询招标公告列表成功",
    "list":[{
	"biderId":3,
	"companyShortName":"3456"
	"companyLogo":"7665",
	"companyRemark":"456"
	}]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
biderId|是|承包商Id
companyShortName|是|公司简称
companyLogo|是|承包商Logo
companyRemark|是|承包商在首页的备注



###查询快捷搜索栏推荐招标项目接口

查询快捷搜索栏推荐招标项目


http请求方式: post

    http://ip:port/if/tender/queryIndexRecommendObjectList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询招标公告列表成功",
    "list":[{
	"objectId":3,
	"objectName":"3456",
	"projectCategoryId":3
	}]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
objectId|是|标的Id
objectName|是|标的名称
projectCategoryId|是|标的类别