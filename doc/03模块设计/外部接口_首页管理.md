#目的
【需要补充完善】

#接口

##查询首页招标项目概况接口

用于首页查询招标项目概况

1）请求说明

http请求方式: post

    http://ip:port/if/tender/queryObjectIndexSurvey


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {
        "errcode":0,"errmsg":"查询首页招标项目概况成功",
        "info":{
            "objectNum":35,
            "amount":"45647689.00"
	   }
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询首页招标项目概况失败，其它错误"}


参数|是否必须|说明
----|----|-----
objectNum|是|正在招标的项目数量
amount|是|正在招标的项目累计交易金额（估价的总金额）


###查询首页招标项目列表接口

用于首页查询招标项目列表。【需要完善，尤其】

http请求方式: post

    http://ip:port/if/tender/queryIndexObjectList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {
        "errcode":0,"errmsg":"查询招标项目列表成功",
        "list":[{
            "objectId":"3456"
            "objectName":"7665",
            "companyShortName":"456",
            "objectEvaluate":30000000,
            "objectPredictStartTime":"2015-03-24",
            "objectPredictEndTime":"2015-08-30"
        }]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
objectId|是|招标项目内部编号
objectName|是|招标项目名称
companyShortName|是|招标人（即公司简称）
||招标人信用等级
objectEvaluate|是|招标项目估价
objectPredictStartTime|是|项目计划开始时间
||工期要求


###查询首页中标结果概况接口

用于首页查询中标结果概况。【补充完善】

http请求方式: post

    http://ip:port/if/bid/queryBidIndexSurvey


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {"errcode":0,"errmsg":"查询首页中标结果概况成功",
     "info":{
         "bidNum":35,
         "amount":45647689
	 }
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询首页中标结果概况失败，其它错误"}


参数|是否必须|说明
----|----|-----
bidNum|是|中标数量
amount|是|中标累计金额


###查询首页中标项目列表接口

用于首页查询中标项目列表。


http请求方式: post

    http://ip:port/if/bid/queryBidIndexList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {
        "errcode":0,"errmsg":"查询招标公告列表成功",
            "list":[{
            "objectName":"7665",
            "objectId":"45657"
            "bideeName":"中标公司简称",
            "biderName":"招标公司简称",
            "objectOffer":"30000000"
        }]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
||中标项目内部编号
objectName|是|中标项目名称
||投标人（即公司简称）
||中标人（即公司简称）
objectOffer|是|中标金额

【按照参数说明的参数修改】

###查询首页投标人概况接口

用于首页查询投标人概况

http请求方式: post

    http://ip:port/if/userInfo/queryBiderIndexSurvey


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
        "body":{}
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {
        "errcode":0,"errmsg":"查询首页投标人概况成功",
        "stairBiderNum":35,
        "secondBiderNum":45        
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询首页招标项目概况失败，其它错误"}


参数|必须|说明
----|----|-----
stairBiderNum|是|一级承包商数量
secondBiderNum|是|二级承包商数量
【名字根据john数据字典定义】


###查询首页投标人推荐列表接口

用于首页查询投标人推荐列表。


http请求方式: post

    http://ip:port/if/userInfo/queryIndexBidList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
        "body":{}
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {
        "errcode":0,"errmsg":"查询招标公告列表成功",
        "list":[{
            "biderId":3,
            "companyShortName":"稳健基础",
            "companyLogo":"LOGO_URL",
            "companyRemark":"深圳最大的地基公司"
        }]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
biderId|是|承包商Id
companyShortName|是|投标人公司简称
companyLogo|是|投标人Logo
companyRemark|是|投标人在首页的备注
【英文名要完善】


##查询快捷搜索栏推荐招标项目接口

用于首页查询快捷搜索栏推荐招标项目


http请求方式: post

    http://ip:port/if/tender/queryIndexRecommendObjectList


POST数据格式：JSON

    {
        "app":{
            "appId":"zjhtwallet",
            "timeStamp":"TIMESTAMP", 
            "nonce":"NONCE",
            "signature":"SIGNATURE"
        }
    } 


参数|是否必须|说明
----|----|-----
appId|是|应用ID
timestamp|是|时间戳
nonce|是|随机数
signature|是|签名值,MD5(按值的字典顺序排列组合成字符串(appId,appKey,app.nonce,app.timeStamp))



2）返回说明

正常时的返回JSON数据包示例：
 
    {
        "errcode":0,"errmsg":"查询快捷搜索栏推荐招标项目成功",
        "list":[{
            "objectId":"BH1245677899",
            "objectName":"深圳湾体育馆工程",
        }]
    }


错误时的JSON数据包示例：

    {"errcode":10000,"errmsg":"查询招标公告列表失败，其它错误"}


参数|是否必须|说明
----|----|-----
objectId|是|招标项目内部编号
objectName|是|招标项目名称
【英文名以john数据字典为准】

##查询快捷搜索栏推荐投标人接口
【补充】

##快捷搜索招标项目接口

##快捷搜索投标人接口

